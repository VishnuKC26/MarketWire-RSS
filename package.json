{
  "name": "market-feed-dashboard",
  "version": "1.0.0",
  "description": "Full-stack aggregated market pulse dashboard",
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "node backend/index.js",
    "dev": "npx concurrently \"node backend/index.js\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
